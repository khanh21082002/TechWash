<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/holo_blue_light" /> <!-- Màu nền khi được chọn -->
    <corners android:radius="16dp" /> <!-- Bo tròn các góc -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black" /> <!-- Viền màu đen -->
    <padding
        android:left="8dp"
        android:top="4dp"
        android:right="8dp"
        android:bottom="4dp" />
</shape>
